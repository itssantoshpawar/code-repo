spring.application.name=osb-pipeline-framework

# Logging
logging.level.com.osb.pipeline=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# Server port (if REST endpoints are used)
server.port=8080
